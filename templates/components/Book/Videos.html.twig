{% if videos is not empty %}
    <twig:c975LUi:Card:Card id="apercu" title="Aperçu">
        {% for video in videos %}
            <twig:c975LBook:Book:Video video="{{ video }}"/>
        {% endfor %}
    </twig:c975LUi:Card:Card>
{% endif %}